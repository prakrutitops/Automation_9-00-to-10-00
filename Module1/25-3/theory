Method Overloading:When two methods having a same name but number of
parameters are diffrent 

Method Overriding:When two methods having a same name and number of parameters are also same then it will be dobe MO

String:

StringBuilder:

StringBuffer:

